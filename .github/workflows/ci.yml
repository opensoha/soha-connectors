name: connectors-ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate connector scaffold
        run: |
          test -f README.md
          test -d connectors
